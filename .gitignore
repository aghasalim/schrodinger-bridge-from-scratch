.venv/
__pycache__/
*.pyc
*.so
.pytest_cache/
.ruff_cache/
.DS_Store
build/
dist/
*.egg-info/
*.ncu-rep

# results/ is deliberately NOT ignored -- benchmark CSVs are committed so the
# README tables stay reproducible.

# verify/ build products. The sources are committed, the binaries are not.
verify/*/target/
verify/gocheck/gocheck
*.class
